The main purpose of the app is so users can upload their notes and generate test from it, it is supposed to save the uploaded note in the library so it can be modified and used to regenerate a test again. Now provided the note has not been modified, when a test regeneration is initiated the note from the library is sent to the input field in dashboard (should in case the user wants to modify the question parameters), the note is sent to the backend to generate a new set of questions and redirects the user to the testpreview page, where they choose the time and see a sample question before they begin the test. Then when the test is initiated the user is taken to the testtaking page where the anser the questions before submitting, after submitting the overall performance of the test is sent back again to the ai to perform analysis on overall performance of the student, it will highlight his weak ppoint, where he needs improvement, comprehension and advice on what to focus next. Now i should note this is all happeneing in the context of the note in question and nothing else. The user/students result are then sent to the results tab and recorded.


Now these are the features i need to add:

I recommend implementing an intelligent content parsing system that automatically generates a structured syllabus from uploaded notes. Instead of requiring users to manually create a course structure, the app should use advanced natural language processing to analyze uploaded content, automatically identify key themes, chapters, and modules, and generate a comprehensive learning framework coupled with the little context the user adds, This approach would transform the current unstructured note-pasting experience into a smart, adaptive learning tool that intelligently breaks down user-uploaded content into meaningful, organized segments. The system would create a draft syllabus with suggested chapters and topics, which users can read, and take tests, ensuring that the app provides a more intuitive and helpful way of organizing learning materials

To complement the intelligent content parsing system, implement a visual document management interface where uploaded files (PDF, DOCX, TXT, MD) are displayed as interactive mini-document containers that can be clicked to preview their contents. This approach provides users with an immediate visual overview of their uploaded learning materials, allows easy document preview, and offers clear feedback on file type support, with specific error messages for unsupported formats. The visual containers would serve as an intuitive layer of interaction before the AI-driven content analysis and syllabus generation, enhancing the overall user experience of document upload and management.

Improve or completelly modify my current notepreview to properly display the structured generated course content in a documentation style arrangement, where each module/chapter has a take a test button, which when clicked makes an api call to generate a test for the specific module/chapter. Default the question generation to 10 queations per module/chapter. and at the end of each course or subject generated add another module or button called take an exam which the user can then take, it should contain all questions generated for that course/subject randomly arranged from all modules/chapters, the exam will only contain a timer no need for a user to set a time limit


Eliminate or repurpose the current testpreview page as i don't think this new setup needs it. Also do the same for all other components in the codebase that these new change affects. 



Simply put user comes to the dashboard and stars or initites the course onboardong by uploading his notes and adding a context (reduce the current dahboard text area text limit to about 500 characters) then proceeds to generate the course which will be automatically opened in the notepreview in library, the user studies the generated note designed like a ocumentation with chapters and subchapters on the left sidebar and content on the right and a take a test button at the bottom of each module/chapter. After the last chapter the user can take an exam and see his overall performance for the specific course in the results tab (make sub modules for each course of subject generated, and have test scores and exam score all shown with a proper calculation method).


Move the current question customization to the settings page, only maintain the question types selection (MCQ or truth/false) and difficulty level.


Now the testresult page should be repurposed or ditched for a testsummary page after each test or exam a student takes it should have the questions the student answered with correct answers indication, a percentage average and an accumulative avearage over the entire course. A retake test button and an AI analyses button, i want the AI analyses to be on request instead of the current automatic system, if a user clicks the request button then the context of the test or exam in question will be sent so the AI can generate real insight based on the context of the test or exam taken, i think this reduces the scope of it's analyses but also improves it's accuracy when returning the analyses
